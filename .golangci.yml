version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign

formatters:
  enable:
    - gofmt

run:
  timeout: 5m

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
